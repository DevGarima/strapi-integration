/** @type {import('next').NextConfig} */
const nextConfig = {
    // "@tailwindcss/postcss": {},
  images: {
      domains: ["localhost"], // added for strapi image 
  },
};

export default nextConfig;
